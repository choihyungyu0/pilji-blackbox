# 필지 블랙박스 — 안양시 위반건축물 우선조사·필지 이력

2026 안양시 공공데이터·AI 활용 대학생 경진대회 시제품. 안양 건물 **27,713동**의 공개 데이터를 AI로 분석해 **위반건축물 우선조사 후보**를 찾고, 필지별 **사고·위반·공사 이력 타임라인**을 한 화면에 보여주고, 담당 공무원이 결재할 **현장조사 기안문·사전통지서 초안(HWPX)** 까지 만드는 웹 서비스.

요구사항 기준 문서: `docs/01_기능명세서.md` > `CLAUDE.md` > `docs/02_아이디어설계서.docx`. 핸드오프 원문은 `docs/00_핸드오프_README.md`.

## 실행

```bash
npm install
cp .env.example .env.local   # 키 채우기 (없어도 앱은 뜬다 — 기능별 비활성 + 사유 표시)
npm run dev                  # http://localhost:3600
```

데이터 재생성(원본 `data/` → `public/data/`, `data/derived/`):

```bash
npm run data:vworld   # 브이월드 API: 개발제한구역·급경사지 필지·지오코딩 (VWORLD_KEY 필요, 1회)
npm run data          # 건물 GeoJSON(공개/담당자)·100m 격자·동별 집계·타임라인 원천
npm run templates     # HWPX 템플릿 (python-hwpx)
```

## 담당자 업무 흐름 (사건 8단계)

담당자가 실제로 일하는 순서 그대로. 각 화살표에는 법정 전제가 있고(`lib/stages.ts canAdvance`), 전제가 안 맞으면 다음 문서는 만들어지지 않는다.

| 단계 | 문서 (HWPX·PDF) | 서식·근거 |
|---|---|---|
| 1 후보 | — | 건축법 79조⑤ 실태조사 대상 (AI 후보 = 현장 확인 전) |
| 2 조사 계획 | 현장조사 계획 기안문 (+붙임 대상 목록) | 일반기안문(행정업무규정 시행규칙 별지1) · 시행령 115조② 목적·기간·대상·방법 |
| 3 현장조사 | 현장조사 결과 보고 (+붙임 조사표) | 시행규칙 40조 결과 기록 · 판정·위반유형·면적·층·사진(EXIF 제거) |
| 4 사전통지 | 처분사전통지서(의견제출통지) | **행정절차법 시행규칙 별지 8호서식 그대로** · 의견제출기한 10일 이상(21조③) · 판정=위반만(BR-C1) |
| 5 시정명령 | 시정명령서 | 79조① · 의견제출기한 경과 또는 의견 접수 후 · 불복 고지(행정절차법 26조) |
| 6 계고 | 이행강제금 부과 계고서 | 80조③ · 시정기한 경과 후 · 산정: 80조①·시행령 115조의3·별표15·**안양시 건축 조례 37조**(감경 1/2, 가중 30%, 연 1회) |
| 7 부과 | 이행강제금 부과 통지 | 80조④ 금액·사유·납부기한·수납기관·이의제기 · **㎡당 시가표준액은 안양시_일반건축물_시가표준액(15080551)에서 자동 입력, 위반면적은 담당자 입력, "참고 산정" 표기**(`scripts/fetch_std_value.py`, 조인 49.1%) |
| 8 종결 | 위반건축물관리대장 | **건축법 시행규칙 별지 29호서식** (건축주·거주자 칸은 미기재) · 사건 전체 CSV |

- 문서 서식은 `data/doc-specs.json` 한 곳에서 정의 → `scripts/make_hwpx_templates.py`(HWPX 템플릿)와 `components/docs/print-doc.tsx`(A4 인쇄 → PDF 저장)가 같은 블록을 그린다.
- 당사자(건축주·거주자) 성명·주소는 어떤 문서에도 자동 기재하지 않는다 — 발송 시 담당자가 직접 기재. 체크리스트가 항상 "누락"으로 남는 이유.
- 사건 기록은 기기(localStorage)에 보관, 판정만 `/api/verdict` 로 서버 동기화 시도. 검증 스크립트: `node scripts/verify_docs.mjs scripts/fixtures/case_2820.json` → `python scripts/verify_docs.py`.

## 화면

| 경로 | 화면 | 모드 |
|---|---|---|
| `/` | WF0 모드 선택 — 공개로 보기 / 담당자 모드 진입(기본 **열림**: 비밀번호 없음, 직위만 선택 입력 → 기안자 칸. `OFFICER_PIN_REQUIRED=1`+`ADMIN_PIN` 이면 PIN, 5회 오류 60초 잠금 — 메모리·서명 쿠키·Supabase 3겹) | 공개 |
| `/work` | 업무 홈 — 오늘 할 일(기한 경과 우선), 단계 파이프라인, 최근 작업 | 담당자 |
| `/map` | WF1 찾기 지도 + WF2 필지 패널 — 위성 배경·3D 건물·레이어·색상 기준·필터·지번 검색·타임라인·주변 현황 · "사건 등록/열기" | 공개(격자) / 담당자(필지 후보·점수) |
| `/investigate` | WF4 조사 계획 — ①후보 뽑기 ②기안문(→조사 계획 단계) ③판정 입력 ④결과 보고, CSV, 번호 핀 | 담당자 |
| `/cases`, `/cases/[id]` | 사건 목록(단계 탭·관리대장 CSV) · 사건 상세(8단계 스테퍼, 단계별 입력·문서·발송일·기한, 이행강제금 계산기, 이력) | 담당자 |
| `/agent` | WF5 에이전트 — 도구 9종(case_status 포함) 대화·근거 조문, 단계 잠금이 걸린 문서 산출 패널 | 담당자 |
| `/print` | A4 인쇄 화면 → 브라우저 "PDF로 저장" | 담당자 |
| `/dashboard` | WF6 성과 — 누적 포착 곡선, 동별 현황, 검증 요약 | 공개 |
| `/about` | WF7 데이터·모델 — 모델 카드, 출처·기준일·라이선스(안양시 9종 포함), 작업 로그 | 공개 |

- **처음 방문자 안내 투어** (`components/app/tour.tsx`, driver.js): 어느 주소로 들어와도 첫 방문이면 랜딩(`/`)으로 보내 화면을 어둡게 하고 요소를 하나씩 강조하며 설명한다. 랜딩 → 지도(시연 필지 박달동 139-137 자동 선택) → 업무 홈 → 조사 계획(목록 자동 생성) → 사건 상세로 이어진다(`?tour=1`). 한 번 본 뒤에는 자동으로 뜨지 않고 상단 "둘러보기"로 화면별 재실행. 플래그는 localStorage(`pb-visited`, `pb-tour-done`).
- 반응형: 640px 미만은 상단 바 압축(아이콘만)·지도 컨트롤 패널 접힘·필지 패널 전체폭 오버레이, 1024px 미만은 패널 340px.


## 안양 여건 보정(ACA) · 판정 기반 재순위 — 순서만 바꾼다

모델 입력 17개는 전부 국토부 건물통합정보에서 나온다. 안양시 공공데이터가 **조사 순서를 실제로 바꾸게** 하려고 사전 계산 `score` 위에 사후 보정을 얹는다(재학습 없음). 확정 수치(27,713 / 1,573 / 4,112 / 1,918 / A 913 / B 1,005 / AUC 0.728·0.698)는 그대로다.

```
adj_score = score × (1 + w_ctx) × (1 + w_verdict)
```

| 코드 | 조건 | 가중 | 데이터 |
|---|---|---|---|
| C1 | 공공건축물 지번 일치 | 후보 제외 | 안양시_공공건축물현황 |
| C2 | 반경 100m 착공신고 | +0.20 | 안양시_건축착공신고현황 |
| C3 / C3′ | 반경 150m 공동주택 ≥1,000 / 500~999세대 | +0.15 / +0.08 | 안양시_공동주택 현황 |
| C4 | 행정동 수방자재(수중+엔진펌프) 하위 25% | +0.10 | 안양시_수방자재 현황 |
| C5 / C6 | 반경 300m 대피시설 / 급수시설 없음 | +0.05 / +0.03 | 안양시_비상대피시설·민방위 급수시설 |
| C7 | 반경 100m 진행 중·예정 도로굴착 (현장 확인 겸행 가능, 위험도 아님) | +0.10 | 안양시_도로굴착 공사현황(오픈API 15152770) |
| C8 | 정비사업 구역(조합설립인가 이후·준공 전 11곳) 반경 200m — 곧 철거 | −0.30 | 안양시_일반 정비사업 추진현황(15150142) |

`w_ctx` 는 [-1.0, +0.60] 절단. `w_verdict = 0.25 × (위반 − 정상) / (위반 + 정상 + 2)` (반경 200m 담당자 판정, 라플라스 평활, HOLD 제외, '대상 아님'은 그 건물만 제외). **판정은 조사 순서만 바꾼다. 위반 여부를 바꾸지 않는다.**

- 규칙: `lib/adjust/rules.ts`(순수) · 빌드 1회 계산: `npm run data:adjust` → `public/data/ctx_adjust.json`(+`data/derived/`) · 서버 정렬: `lib/adjust/server.ts`(/api/candidates) · 클라이언트: `lib/adjust/use-ranking.ts`(판정이 바뀌면 즉시 재정렬) · 판정 좌표: `/api/adjust/verdict`
- 화면: 필지 패널 "AI 점수 → 조사 순위 점수 (여건 +N% · 판정 ±N%)" + 사유 칩, 조사 목록 기본/보정 순위 열(▲▼)·정렬 토글·판정 반영 배너, 성과 대시보드 "안양시 데이터가 바꾼 조사 순서" 카드, 지도 후보색(제외 반영·보정 점수 색)
- **끄기**: `NEXT_PUBLIC_ADJ_ENABLED=false` → 모든 보정·UI 가 사라지고 보정 전과 똑같이 동작. 데이터 누락·API 실패는 조용히 기본 점수로 폴백.

## 구현 범위

P0 전부 구현. P1 중 구현: DAT-06 도로굴착(안양시 오픈API, 빌드 캐시 55건·EPSG:5186→4326)·DAT-07 지반침하(국토부 지하안전정보, 안양 6건) — 타임라인 50m·여건·지도 레이어·보정 C7, TML-02 이벤트 유형 칩, PCL-03 주변 현황(위반·미연계·급경사지·시설), **PCL-04 필지 여건**(`lib/context.ts` — 어느 필지든 행정동·수방자재·개발제한구역·급경사지·공공건축물·대피/급수시설·공동주택 단지·착공신고·사고·이웃 위반을 반경 대조 + 적용 법령; 필지 패널·사건 상세·에이전트 timeline_build 에 공통), SEC-04 작업 로그(기기 보관 + Supabase 선택), DAT-09 시설 4종 지오코딩(공공건축물 450·대피 213·급수 45·공동주택 176).
P1 미구현: DAT-08 CCTV, SAT-01~03 위성(대상 626동·10m 해상도·STAC 차단 — 채택 안 함), SIG-02 지목 불일치, SIG-03 사면(DEM — 급경사지 검증에서 가설 기각, 성과 화면 검증 카드), MDL-02 재학습, INV-03 동선, DOC-03 급경사지 점검 요청, MAP-06 연도 슬라이더. 레이어 토글은 "미적재"로 비활성 표시, 에이전트 도구는 `available:false` 로 응답.

## 스택 (CLAUDE.md 대비 변경·임시 결정)

- Next.js 15(App Router) + TypeScript + Tailwind 4 — Homepage 프로젝트 도구 설정·폰트(Pretendard)·UI 토큰 계승
- **지도: MapLibre GL JS 단독 (deck.gl 미사용)** — 27,713동 3D 돌출은 MapLibre 내장 `fill-extrusion`(높이 = 층수×3m → 높이 → 3m)이 의존성 없이 더 가볍고, `feature-state` 로 선택·판정 색을 즉시 반영하며 GeoJSON 소스가 타일 분할을 자동으로 한다. deck.gl 상호운용 층을 두는 이득이 없어 제외.
- **IS-01 임시 결정: PMTiles 대신 정적 GeoJSON** — Windows 작업환경에 tippecanoe 가 없어 `public/data/{public,officer}/buildings.geojson`(각 15.9/18.7MB, gzip 2.6/3.0MB)을 Vercel 정적 파일로 제공. 로컬 첫 로드 1.4초(캐시 후 ~0.2초). 좌표 6자리·NaN→null 정리.
- **IS-09 공개 범위: 파일 자체를 분리** — 공개 파일에는 `score/cand/grade/f_*` 컬럼이 없고, 담당자 파일(`/data/officer/*`)은 미들웨어가 세션 쿠키로 막는다. 화면 숨김이 아니라 네트워크에서 차단.
- **IS-07 LLM: OpenAI `gpt-4o-mini`(환경변수)** 도구 호출 9종. BR-A1 은 `lib/guard.ts` 로 답변 속 숫자를 도구 결과와 대조해 없는 문장을 삭제.
- **IS-08 법령 원문: 국가법령정보센터 현행 조문 15개를 `data/laws/laws.json` 에 수록**(2026-09-17~18 조회·2026-09-18 재대조) — 건축법 79·80·80조의2, 시행령 115·115조의2·115조의3, 시행규칙 40, 행정절차법 14·21·26·27, 개발제한구역법 30·30의2, 급경사지법 시행령 2, 안양시 건축 조례 37. 키워드 점수 검색(임베딩 없음).
- **IS-10 문서 서식: 표준 서식으로 자체 제작** (안양시 실제 서식 미확보). python-hwpx 로 만든 정품 HWPX 템플릿에 브라우저(JSZip)가 토큰만 치환. 사전통지서는 행정절차법 21조① 기재사항 순서, 당사자 칸은 항상 공란.
- **IS-11 서비스 이름: "필지 블랙박스"(가칭 유지)**.
- Supabase 는 선택 — 판정 저장 실패 시 기기(localStorage) 보관 + 재시도(ST-V3). 스키마 `supabase/schema.sql`.
- 브이월드 키: 개발키(2026-09-18 발급, 만료 2027-03-18), 서비스 URL `https://pilji-blackbox.vercel.app`. WMTS·2D데이터·지오코더 사용. 영상 타일은 조회만.

## 데이터 (기준 2026-09-09) — 확정 수치 재현 확인

건물 27,713 · 위반 표기 1,573 · 대장 미연계 4,112 · 후보 1,918(A 913 / B 1,005) · 1년 변화 신규 88·해제 64 · 공간 AUC 0.728 / 시간 AUC 0.698 · 상위 20% 포착 46.8% / 46.6%. `scripts/build_data.py` 가 적재 시 이 수치를 assert 한다.

추가 산출: 개발제한구역 31 폴리곤(건물 626동 내부) · 급경사지 47행 중 45행 위치화(필지 38개; 실패: 호계3동 안양교도소 지번 없음, 만안 안양 N10지구 연속지적도에 PNU 없음) · 사고 8건 중 좌표 3건(INC-04·06·07; 나머지는 지점 주소 없음, 목록만) · 시설 지오코딩 공공건축물 450/537, 대피시설 213/213, 급수시설 45/46.

## 폴더

```
app/            (map | investigate | agent | dashboard | about) · api/(auth|parcel|timeline|candidates|verdict|doc|laws|agent)
components/     map/ parcel/ investigate/ agent/ dashboard/ app/
lib/            data-server(정적 인덱스) · timeline · laws · docs · stages(사건 단계·이행강제금) · guard(BR-A1) · pii · session · hwpx · tools/(에이전트 도구 9종)
store/          app-store(모드·레이어·조사목록·기관정보·로그, 기기 보관) · cases(사건 8단계) · buildings(건물 인덱스)
data/           핸드오프 원본 + derived/(빌드 산출) + laws/
public/data/    앱이 읽는 정적 파일 (officer/ 는 미들웨어 보호)
public/templates/ HWPX 템플릿 7종
scripts/        build_data.py · fetch_vworld_layers.py · make_hwpx_templates.py · analysis_reference/(모델 재현 코드)
docs/           기능명세서·설계서·데이터사전·시연 시나리오·리서치
```

## 원칙 (절대 금지 — CLAUDE.md)

소유자·거주자 정보 없음(메모 전화·주민번호 패턴 차단, 사진은 EXIF 제거 썸네일만 기기 보관) · 사전통지는 판정=위반일 때만 · 공개 모드 필지 단위 후보 없음 · 도구에 없는 수치 삭제 · 도구 실패 명시 · 박달동 축대 붕괴 원인 단정 없음 · 브이월드 타일 저장 없음 · 확정 수치 변경 없음.
